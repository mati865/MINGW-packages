machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "mingw-builds"

test:
  override:
#    - docker pull "fracting/msys32" 
    - docker run "fracting/msys32" "/usr/bin/msys32" "./ci-build.sh" 
#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#- docker push sandcastle/swagger-codegen-docker